# C4 Architecture — Contract Skill Pack MVP

This document shows the entities that participate in the current MVP. OpenContracts contains only two runtime retrieval corpuses: `contracts-history` and `contract-templates`. Session experience is a separate local/manual maintenance flow and never enters OpenContracts automatically.

## Level 1 — System Context

```mermaid
C4Context
title Contract Skill Pack MVP — System Context

Person(user, "Contract User", "Creates, reviews, modifies and queries contracts through a Harness")
Person(maintainer, "Skill Maintainer", "Periodically reviews experience notes and publishes reviewed Skill changes")

System(harness, "WorkBuddy / Compatible Harness", "Conversation runtime, model access, local files, artifacts and installed Contract Skill Pack")
System(opencontracts, "OpenContracts", "Historical contracts, contract templates, MCP retrieval and controlled formal ingestion")
System_Ext(git, "ContractBotConfig Git Repository", "Versioned Skill source, deployment scripts and configuration templates")

Rel(user, harness, "Uses")
Rel(harness, opencontracts, "Optional retrieval and explicit formal ingestion", "HTTPS via Caddy on fixed LAN IP")
Rel(harness, git, "Loads/releases reviewed Skill Pack")
Rel(maintainer, git, "Reviews and updates Skills")
Rel(maintainer, harness, "Collects approved local experience notes from the operating environment")
```

## Level 3 — Runtime Components and Full Flow

```mermaid
C4Component
title Contract Skill Pack MVP — Runtime Components and Data Flow

Person(user, "Contract User", "Provides current files/facts and authorizes optional remote operations")
Person(maintainer, "Skill Maintainer", "Reviews experience notes and manually updates Skills")

Container_Boundary(agent, "WorkBuddy / Harness Host") {
    Container(runtime, "Harness Runtime", "WorkBuddy / compatible Harness", "Conversation state, model execution, MCP client, local file access")
    Component(contract, "contract", "Skill", "Default contract-mode routing and behavior")
    Component(repository, "contract-repository", "Skill", "Historical contract and template retrieval with evidence tracking")
    Component(upload, "contract-upload", "Skill", "Explicit formal-ingestion policy and duplicate/write-state handling")
    Component(document, "contract-document", "Skill", "Formal document structure and editable artifact rules")
    Component(learning, "contract-learning", "Skill", "Creates local experience notes after separate consent")
    Component(uploadHelper, "upload_document.py", "Python helper", "Deterministic WorkerKey-authenticated formal upload; no automatic retry")
    ContainerDb(localFiles, "Local Files / Artifacts", "Harness workspace", "Current contracts, drafts, generated DOCX/artifacts")
    ContainerDb(experience, "Experience Notes", "Local/shared Markdown", "contract-experience-note.md files awaiting human review")
    ContainerDb(runtimeConfig, "Runtime Config / Secret Store", ".mcp.json + environment + Windows trust", "Fixed-IP URLs, two Corpus slugs, Caddy CA path and history WorkerKey")
}

Container_Boundary(ocHost, "OpenContracts Host — Trusted LAN / VPN") {
    Container(caddy, "Caddy", "HTTPS reverse proxy", "Serves https://<fixed-lan-ip>:443 with tls internal")
    Container(mcp, "OpenContracts MCP", "HTTP MCP /mcp/", "Anonymous reads of public retrieval corpuses")
    Container(importApi, "OpenContracts Import API", "REST /api/imports/documents/", "Accepts formal uploads authenticated by corpus-bound WorkerKey")
    Container(processing, "OpenContracts Processing / Indexing", "Django/Celery pipeline", "Parses submitted documents and makes text/search data available")
    ContainerDb(history, "contracts-history", "OpenContracts Corpus", "Historical/draft contracts used for comparison and retrieval")
    ContainerDb(templates, "contract-templates", "OpenContracts Corpus", "Enterprise contract templates used for drafting")
}

System_Ext(git, "ContractBotConfig Git Repository", "Versioned Skills, deployment scripts and configuration templates")

Rel(user, runtime, "Uses")
Rel(runtime, contract, "Loads for contract work")
Rel(contract, localFiles, "Reads current files / writes local drafts")
Rel(contract, repository, "Loads when history/template retrieval is requested or accepted")
Rel(contract, upload, "Loads only after explicit formal-ingestion intent")
Rel(contract, document, "Uses for formal document output")
Rel(contract, learning, "Loads after separate experience-capture consent")

Rel(repository, runtime, "Uses Harness MCP client")
Rel(runtimeConfig, runtime, "Provides MCP URL, Corpus slugs and trusted CA configuration")
Rel(runtime, caddy, "MCP requests", "HTTPS")
Rel(caddy, mcp, "Reverse proxies /mcp/")
Rel(mcp, history, "Searches/lists/reads")
Rel(mcp, templates, "Searches/lists/reads")
Rel(repository, contract, "Returns Reference Pack / grounded source material")

Rel(document, localFiles, "Creates/updates local artifacts")

Rel(upload, uploadHelper, "Invokes after duplicate check and user authorization")
Rel(runtimeConfig, uploadHelper, "Provides base URL, trusted CA and history WorkerKey")
Rel(uploadHelper, caddy, "Formal upload", "HTTPS + WorkerKey")
Rel(caddy, importApi, "Reverse proxies /api/imports/documents/")
Rel(importApi, processing, "Submits accepted document")
Rel(processing, history, "Stores/processes formal contract")
Rel(mcp, runtime, "Later verification of searchable/readable state")

Rel(learning, experience, "Writes local experience note")
Rel(maintainer, experience, "Periodically collects and reviews")
Rel(maintainer, git, "Manually edits reviewed Skills")
Rel(git, runtime, "Next reviewed Skill version is installed/released")
```

## Boundaries represented in the diagrams

- **Local-first boundary:** current user files, drafts and experience notes remain on the Harness side unless formal ingestion is explicitly authorized.
- **Network boundary:** OpenContracts is reachable only through the trusted LAN/VPN at a fixed private IP; Caddy is the intended network-facing HTTPS endpoint.
- **Read boundary:** MCP reads are anonymous in the MVP because `contracts-history` and `contract-templates` are public inside the trusted network.
- **Write boundary:** `OPENCONTRACTS_UPLOAD_WORKER_KEY` is bound to `contracts-history` and is only used by the deterministic upload helper.
- **Learning boundary:** experience notes are reviewed by a human maintainer and become future behavior only through normal Git-reviewed Skill changes.

Channel-specific integrations such as WeCom/WeChat are Harness concerns and are intentionally outside the Contract Skill Pack boundary shown here.

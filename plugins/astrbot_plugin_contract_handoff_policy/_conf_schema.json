{
  "long_task_ack_enabled": {
    "description": "独立合同库读取委派前发送一条处理中提示",
    "type": "bool",
    "default": true
  },
  "long_task_ack_text": {
    "description": "合同库读取的单次处理中提示",
    "type": "string",
    "default": "正在读取合同库中的相关合同，完成后会继续在当前会话返回结果。"
  },
  "default_opencontracts_corpus_slug": {
    "description": "没有 Router 任务上下文时使用的默认合同库 Corpus slug；用于只读任务，避免让 Agent 调用 list_public_corpuses 猜库",
    "type": "string",
    "default": "contracts"
  }
}

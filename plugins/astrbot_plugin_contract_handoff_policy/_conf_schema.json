{
  "long_task_ack_enabled": {
    "description": "独立合同库读取委派前发送一条处理中提示",
    "type": "bool",
    "default": true
  },
  "long_task_ack_text": {
    "description": "合同库读取的单次处理中提示",
    "type": "string",
    "default": "正在读取合同库中的相关合同，完成后会继续在当前会话返回结果。"
  },
  "default_opencontracts_corpus_slug": {
    "description": "OpenContracts 默认合同库 Corpus slug；独立读取、上传发现与 Builder 生成参考读取共用这一配置，避免 Agent 猜测 Corpus",
    "type": "string",
    "default": "contracts"
  }
}
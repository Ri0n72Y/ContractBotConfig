{
  "base_url": {
    "description": "Docassemble 服务根地址；AstrBot 与 Docassemble 同网时使用容器地址",
    "type": "string",
    "default": "http://docassemble"
  },
  "api_key": {
    "description": "Docassemble API Key；MVP 暂可使用管理员账户 Key，禁止进入 LLM 上下文或日志",
    "type": "string",
    "default": "",
    "obvious_hint": true
  },
  "allowed_interviews": {
    "description": "允许 ContractBot 执行的完整 interview filename 白名单",
    "type": "list",
    "default": []
  },
  "default_interview": {
    "description": "默认 interview filename；必须同时存在于 allowed_interviews",
    "type": "string",
    "default": ""
  },
  "result_descriptor_key": {
    "description": "Interview 最终 JSON 中的文件描述对象路径",
    "type": "string",
    "default": "contractbot_document"
  },
  "output_dir": {
    "description": "从 Docassemble 取回的 DOCX 本地保存目录；仅作为短期生成源目录",
    "type": "string",
    "default": "data/plugins_data/astrbot_plugin_docassemble_gateway/output"
  },
  "timeout_seconds": {
    "description": "Docassemble API 单次请求超时秒数",
    "type": "int",
    "default": 90
  },
  "max_file_bytes": {
    "description": "允许取回的单个 DOCX 最大字节数",
    "type": "int",
    "default": 31457280
  },
  "verify_tls": {
    "description": "HTTPS 证书验证；HTTP 容器网络不受影响",
    "type": "bool",
    "default": true
  },
  "cleanup_sessions": {
    "description": "每次生成完成或失败后删除一次性 Docassemble session",
    "type": "bool",
    "default": true
  },
  "output_retention_seconds": {
    "description": "Gateway 本地生成 DOCX 的保留时间（秒）；默认 24 小时",
    "type": "int",
    "default": 86400
  },
  "output_cleanup_interval_seconds": {
    "description": "后台扫描并删除过期 Gateway DOCX 的间隔（秒）",
    "type": "int",
    "default": 300
  }
}

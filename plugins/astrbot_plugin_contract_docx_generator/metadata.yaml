name: astrbot_plugin_contract_docx_generator
display_name: 合同 DOCX 生成器
desc: 将 Builder 的完整合同 Markdown 一次生成可编辑 DOCX，按 generation_id 幂等；支持显式 generation_basis、source draft 版本来源和成功交付后的草稿 lineage 持久化。
version: 0.5.2
author: local
repo: ""
astrbot_version: ">=4.27.0,<5"
support_platforms:
  - wecom

# Contract Upload Deployment

This file is overwritten by `deploy/opencontracts/Prepare-WindowsClientBundle.ps1` for the active server.

IMPORT_URL=http://10.10.20.15/api/imports/documents/

The client must not send an Authorization header. Caddy injects the corpus-bound WorkerKey server-side.
